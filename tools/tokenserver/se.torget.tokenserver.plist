<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!-- Tokenmätarens tjänst som launchd-agent: startar vid inloggning och
     återstartas om den dör.

     DEN HÄR FILEN ÄR EN MALL, inte något att kopiera rakt av. Sökvägarna
     nedan är från när projektet hette Torget (~/Torget, användaren
     niclasvestlund) och stämmer bara av en slump. Kopierar man den till en
     klon som ligger någon annanstans pekar WorkingDirectory fel och
     `launchctl kickstart` svarar "Could not find service" utan att säga
     varför. Mallen saknar dessutom relä-flaggan (den skrivs inte ut här:
     ett XML-kommentar får inte innehålla två bindestreck i rad), så en
     tjänst installerad härifrån serverar LAN men publicerar ALDRIG till
     reläet — och inget felar, brevlådan blir bara gammal.

     Kör i stället tools/tokenserver/install-launchd.sh, som fyller i
     sökvägarna från sin egen plats och tar relä-URL:en som argument
     (macOS-motsvarigheten till install-windows-task.ps1).
     Interaction providers and detail come from the tokenserver's saved config;
     keep them out of ProgramArguments so setup changes cannot go stale here.
     Loggen bor i ~/Library/Logs (överlever omstart, syns i Konsol-appen;
     /tmp städas av macOS). Servern roterar den själv vid start när den
     vuxit förbi taket — launchd har ingen egen rotation.
     ThrottleInterval är staketet mot respawn-loopar: dör processen direkt
     vid start väntar launchd 30 s i stället för att fylla loggen. -->
<plist version="1.0">
<dict>
  <key>Label</key>
  <string>se.torget.tokenserver</string>
  <key>ProgramArguments</key>
  <array>
    <string>/usr/bin/python3</string>
    <string>-u</string>
    <string>tokenserver.py</string>
  </array>
  <key>WorkingDirectory</key>
  <string>/Users/niclasvestlund/Torget/tools/tokenserver</string>
  <key>RunAtLoad</key>
  <true/>
  <key>KeepAlive</key>
  <true/>
  <key>ThrottleInterval</key>
  <integer>30</integer>
  <key>StandardOutPath</key>
  <string>/Users/niclasvestlund/Library/Logs/torget-tokenserver.log</string>
  <key>StandardErrorPath</key>
  <string>/Users/niclasvestlund/Library/Logs/torget-tokenserver.log</string>
</dict>
</plist>
